# Local runtime settings (do not store real secrets in Git)
AGENT_ID=codex
SESSION_METHOD=isolated_profile
USER_DATA_DIR=./user_data_codex

# Optional browser overrides
BROWSER_CHANNEL=
BROWSER_EXECUTABLE_PATH=
